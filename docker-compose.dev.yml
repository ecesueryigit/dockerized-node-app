services:

  node-app:
    container_name: devops-node-app

    build:
        context: ./app

    ports:
        - "3000:3000"

    volumes:
        - ./app:/app
        - /app/node_modules
        - ./data:/app/data

    env_file:
        - .env 
      
    command: npm run dev

      
