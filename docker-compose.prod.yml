 services:
  
    node-app:
      container_name: devops-node-app-prod

      build:
        context: ./app
        dockerfile: Dockerfile

      ports:
        - "3000:3000"


      env_file:
        - .env

      command: npm start
