groups:
  - name: node-app-alerts
    rules:
      - alert: NodeAppDown
        expr: up{job="node-app"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Node.js application is down"
          description: "Prometheus cannot reach the node-app target."